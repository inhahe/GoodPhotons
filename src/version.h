#pragma once
// The repo-root VERSION file's string, baked in by CMake -- but into version.cpp ONLY, so a
// version bump recompiles that one five-line file instead of every translation unit.
const char* ftraceVersion();
