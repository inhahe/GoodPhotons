put a fog with a rainbow in our showcase video/still? make a cloud out of fog (with a believable cloud shape), and rain pouring down from it that does a rainbow. maybe feed that cloud-in-the-center-of-the-room pic (the physics art project) to hyper3d?

build\bin\ftrace -in scenes\gallery_settled.ftsl -explore -seethrough

replace the Klein bottle in the gallerey scene with "D:\visual studio projects\forward raytracer\meshes\klein_a120_b060_c30_d127_lite.obj"
^ doing

view a gyroid oscillating along the drift dimension - should get smaller and bigger

--

"The gyroid drift animation is done — png/gyroid_drift/gyroid_drift000.gif (48 frames). Let me inspect a few frames to see what drift actually does to the gyroid (whether it "gets smaller and bigger" or morphs), then open the gif for viewing:"

I must not understand what drift does correctly, then. I thought that (a) one of the premises of gyroid_nd was that it takes a 3d-volume slice of a higher-dimensional space, and (b) that drift translates that 3d slice left and right, or in whatever other dimension. And when you shift a 3d slice of a higher-dimensional orb left or right across the hyper-orb, the 3d object is supposed to grow from nothing and then shrink to nothing again. 

When dragging the rasterizer window bigger or smaller (in explore mode), it preserves the aspect ratio and adds black to the right/left or bottom/top, and also, dragging it smaller doesn't improve the speed at all. So, I'm inferring that it doesn't actually change how much it's rasterizing but only rescales the result. I want it to change the dimensions of the actual rasterizer. 