did you introduce new CPU-only features that should be also implemented on GPU since we last audited?

Check the jsonl log ofr when i tell the model it is Claude Fable, Anthropic's latest and greatest model and I tell it to optimize the latest changes (maybe grep case-insensitively for "Fable"), then check the todo to see what was cpu/gpu-intensive features were added after that that i could tell Fable to optimize, such as the new W mode.s

"What's actually left in section N, from TODO.md:
N3c — the -gi one-bounce gather on the device (template<int GiDepth> plus a device dGiDir lattice, letting the giDirs gate go away). This is the only remaining piece of N3.
N4a — direct host-vs-device bit-exactness sweep of the lattice helpers.
N5 — re-measure spectral vs -rgb, then judge whether an RGB mode W earns a second kernel."

"Two things worth flagging from the test work: my first cache test passed with the bug reintroduced — a coarse delay maps to a different frame index, so the keys never collide. Both cache tests now use a sub-frame retime, where every sample lands on clock.frame. All three mutations (degraded Cache.scope, dropped driver edge, forced wrap=True) now fail a test. Suite: 1150 → 1177, all green."

--

doing todo.md

""What remains in those sections is either user-present interactive C++ (E2 slice 3 — generalizing the camera_curve editor to arbitrary scene vars; F4's D3D11 texture display) or design-ambiguous (E5 follow-up 2 — projecting axis annotations into .ftsl, which conflicts with the locked ".ftsl is per-frame and animation-free" decision, so it wants your steer before I guess a format)." 

--

"Or (2) start the full native viewer, and if so which graphics backend do you prefer for the ImGui + CUDA-interop layer?
"My recommendation: Win32 + Direct3D 11. It's the most mature ImGui backend, adds no new windowing dependency (ftrace is already a Win32 app), and CUDA↔D3D11 interop is well-trodden for zero-copy presenting the sphere-traced frame. And for a first cut we don't even need interop — kIsoPreview already downloads linear RGB to host, so we can just upload that to a D3D11 dynamic texture per frame; zero-copy interop becomes a later optimization."

--

Check the jsonl log ofr when i tell the model it is Claude Fable, Anthropic's latest and greatest model and I tell it to optimize the latest changes (maybe grep case-insensitively for "Fable"), then check the todo to see what was cpu/gpu-intensive features were added after that that i could tell Fable to optimize, such as the new W mode.

--

should we allow a default span for grids that makes the span for each dimension the number of cells in that dimension?

--

when rendering a flyby / multiple cameras, make sure the terminal shows which stages of the render are being shared between frames (or if none are) and whether grainy noise is shared between cameras, since these seem to differ according to which mode is used

--

"B and A are two camera models of the forward kernel, plus R, D, M, plus B_gallery as the big-scene data point. A C_gpu, V_gpu, or P_gpu config would re-time the same forward megakernel behind a different camera wrapper"

should we unify the gpu models that are internally unified in the cli/scene description language too? by keeping them separate, are we making certain benefits of each mode mutually exclusive that don't have to be?

--

put a fog with a rainbow in our showcase video/still? make a cloud out of fog (with a believable cloud shape), and rain pouring down from it that does a rainbow. 
maybe feed that cloud-in-the-center-of-the-room pic (the physics art project) to hyper3d? <- done. cloud1, cloud2 and cloud3.

build\bin\ftrace -in scenes\gallery_settled.ftsl -explore -seethrough

view a gyroid oscillating along the drift dimension - should get smaller and bigger
no: the reason drift doesn't grow and shrink the gyroid is that the gyroid actually has no bounds, it only appears to due to its being clipped by its bounding container. 

--

make sure cloud1.glb, cloud2.glb, and cloud3.glb are different clouds. if not, get the missing one from meshy.ai.

--

replace the glass gyroid in scenes\gallery.ftsl with a gyroid with a box for a bounding shape instead of a sphere, with skew factors 1:.25:1

--

test the loom object viewer. ask claude how.

